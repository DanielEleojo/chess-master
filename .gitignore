node_modules/
dist/
.wrangler/
.dev.vars

# Account state — written at runtime by the data middleware (dev) or KV (prod),
# one account's own games and stats. data/repertoire.pgn is NOT here: it's
# authored seed content, and ticket 020 writes accepted line extensions into it.
data/*.json
data/archives/

# Source dump for scripts/build-puzzles.py — 291MB, rebuilt on demand
data/lichess_db_puzzle.csv.zst

# Generated by `wrangler types` (run by npm run check)
worker-configuration.d.ts
